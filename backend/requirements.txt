# Local install entry (Windows dev machines). Containers use requirements.lock via Dockerfile.
# The lock is generated with `uv pip compile --universal`: it carries both Windows and Linux
# hashes, and uvloop is installed only on non-Windows platforms (see requirements.in notes).
-r requirements.lock
